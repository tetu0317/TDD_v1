# TC_005: 検索結果なしテスト

## テストケース情報

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC_005 |
| **テスト分類** | 単体テスト |
| **対象クラス** | DocumentSearcher |
| **対象メソッド** | `search(keyword)` |
| **優先度** | 中 |

## テスト内容

### 事前条件（試験規格）
- ドキュメントが登録済み

### 判定条件（試験合格値）
- 存在しないキーワードで検索時、空配列が返却される

## 検証項目

1. `search("存在しないキーワード")`の結果が空配列`[]`
2. 結果の長さが0

## テストコード例

```javascript
test('存在しないキーワードで検索時、空配列が返却される', () => {
  const searcher = new DocumentSearcher();
  searcher.addDocument('設計書.txt', '/documents/design.txt', 'システム設計に関する文書です。');

  const results = searcher.search('存在しないキーワード');

  expect(Array.isArray(results)).toBe(true);
  expect(results).toHaveLength(0);
  expect(results).toEqual([]);
});
```

## TDD実施履歴

### 🔴 Red（失敗するテストを書く）

**実施日**: 2025-11-19

**実施内容**:
1. テストコードを作成: `test/単体テスト/DocumentSearcher/TC_005.test.js`
2. テスト実行
   ```
   FAIL test/単体テスト/DocumentSearcher/TC_005.test.js
   expect(received).toHaveLength(expected)
   Expected length: 0
   Received length: 1
   ```
3. 結果: ❌ テスト失敗（期待通り）

### 🟢 Green（テストを通す最小限の実装）

**実施日**: 2025-11-19

**実施内容**:
1. `search()`メソッドはTC_003で既に実装済み
   - filterメソッドは該当がない場合、自動的に空配列を返す
   - 追加実装不要

2. テスト実行
   ```
   PASS test/単体テスト/DocumentSearcher/TC_005.test.js
   ✓ 存在しないキーワードで検索時、空配列が返却される (1 ms)
   ```

3. 結果: ✅ テスト成功

### 🔵 Refactor（リファクタリング）

**実施日**: 2025-11-19

**実施内容**:
- filterメソッドの仕様により自然に空配列が返る
- テスト再実行で継続的に成功を確認

**最終結果**: ✅ 全テスト成功

### テスト実行サマリー

| フェーズ | 状態 | 実施日 |
|---------|------|--------|
| Red | ✅ 完了 | 2025-11-19 |
| Green | ✅ 完了 | 2025-11-19 |
| Refactor | ✅ 完了 | 2025-11-19 |

## 関連ドキュメント

- 実装ファイル: `src/DocumentSearcher.js`
- テストファイル: `test/単体テスト/DocumentSearcher/TC_005.test.js`
