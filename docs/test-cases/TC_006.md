# TC_006: 空文字検索テスト

## テストケース情報

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC_006 |
| **テスト分類** | 単体テスト |
| **対象クラス** | DocumentSearcher |
| **対象メソッド** | `search(keyword)` |
| **優先度** | 中 |

## テスト内容

### 事前条件（試験規格）
- ドキュメントが登録済み

### 判定条件（試験合格値）
- 空文字で検索時、全ドキュメントが返却される

## 検証項目

1. `search("")`の結果が全ドキュメントの配列
2. 結果の長さが登録済みドキュメント数と一致

## テストコード例

```javascript
test('空文字で検索時、全ドキュメントが返却される', () => {
  const searcher = new DocumentSearcher();
  searcher.addDocument('設計書.txt', '/documents/design.txt', 'システム設計に関する文書です。');
  searcher.addDocument('仕様書.txt', '/documents/spec.txt', '機能仕様とシステム要件を記載。');
  searcher.addDocument('議事録.txt', '/documents/minutes.txt', '会議の内容と決定事項。');

  const results = searcher.search('');

  expect(Array.isArray(results)).toBe(true);
  expect(results).toHaveLength(3);
  expect(results.length).toBe(searcher.documents.length);
});
```

## 実装コード例

```javascript
search(keyword) {
  // 空文字の場合は全ドキュメントを返す
  if (keyword === '') {
    return this.documents;
  }

  return this.documents.filter(doc => {
    return doc.title.includes(keyword) ||
           doc.path.includes(keyword) ||
           doc.content.includes(keyword);
  });
}
```

## テスト実行結果

- [ ] テスト実行前（Red）
- [ ] テスト成功（Green）
- [ ] リファクタリング完了（Refactor）

## 関連ドキュメント

- 実装ファイル: `src/DocumentSearcher.js`
- テストファイル: `test/単体テスト/DocumentSearcher/TC_006.test.js`
